# =============================================================================
# OpenAI API 설정
# =============================================================================
# OpenAI API 키는 LLM(대형 언어 모델)과 임베딩 모델을 사용하기 위해 필요합니다.
# OpenAI 웹사이트(https://platform.openai.com/api-keys)에서 발급받을 수 있습니다.
# 주의: 이 키는 절대 공개 저장소에 커밋하지 마세요. .env 파일은 .gitignore에 포함되어 있습니다.
OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# 벡터 데이터베이스 설정
# =============================================================================
# 벡터 DB는 법률 문서의 임베딩 벡터를 저장하고 검색하는 데 사용됩니다.

# ChromaDB (로컬 저장소) 설정
# ChromaDB는 로컬 파일 시스템에 벡터 데이터를 저장하는 오픈소스 벡터 DB입니다.
# 상대 경로 또는 절대 경로를 지정할 수 있습니다.
# 예: ./data/vector_db 또는 C:/Users/username/data/vector_db
# 이 디렉토리는 자동으로 생성됩니다.
CHROMA_PERSIST_DIRECTORY=./data/vector_db

# Pinecone (클라우드 벡터 DB) 설정 - 선택사항
# Pinecone을 사용하려면 아래 주석을 해제하고 설정하세요.
# ChromaDB와 Pinecone 중 하나만 사용하면 됩니다.
# Pinecone은 확장성이 뛰어나고 관리형 서비스입니다.
# 
# Pinecone API 키: https://app.pinecone.io/ 에서 발급받을 수 있습니다.
# PINECONE_API_KEY=your_pinecone_api_key_here
# 
# Pinecone 환경: 사용 중인 Pinecone 환경 이름 (예: us-west1-gcp, us-east-1)
# PINECONE_ENVIRONMENT=your_environment
# 
# Pinecone 인덱스 이름: 벡터 데이터를 저장할 인덱스의 이름
# Pinecone 대시보드에서 미리 생성해야 합니다.
# PINECONE_INDEX_NAME=ibs-legal-ai

# =============================================================================
# LLM (Large Language Model) 설정
# =============================================================================
# LLM 모델은 사용자 질문에 대한 답변을 생성하는 데 사용됩니다.
# 
# 사용 가능한 모델:
# - gpt-4-turbo-preview: 최신 GPT-4 Turbo 모델 (권장, 고품질 응답)
# - gpt-4: 표준 GPT-4 모델
# - gpt-3.5-turbo: 더 빠르고 저렴하지만 품질이 낮음
# 
# 모델 변경 시: OpenAI API에서 지원하는 모델 이름을 정확히 입력하세요.
LLM_MODEL=gpt-4-turbo-preview

# 임베딩 모델 설정
# 임베딩 모델은 법률 문서와 검색 쿼리를 벡터로 변환하는 데 사용됩니다.
# 
# 사용 가능한 모델:
# - text-embedding-3-large: 최신 대형 임베딩 모델 (권장, 높은 정확도)
# - text-embedding-3-small: 더 작고 빠른 모델 (비용 절감)
# - text-embedding-ada-002: 이전 버전 모델
# 
# 참고: 임베딩 모델은 검색 정확도에 직접적인 영향을 미칩니다.
EMBEDDING_MODEL=text-embedding-3-large

# =============================================================================
# API 서버 설정
# =============================================================================
# FastAPI 서버의 네트워크 및 동작 설정입니다.

# API 호스트 주소
# 0.0.0.0: 모든 네트워크 인터페이스에서 접근 가능 (프로덕션 권장)
# 127.0.0.1 또는 localhost: 로컬에서만 접근 가능 (개발 환경)
# 
# 주의: 프로덕션 환경에서는 방화벽 및 보안 그룹 설정을 확인하세요.
API_HOST=0.0.0.0

# API 포트 번호
# 서버가 수신 대기할 포트 번호입니다.
# 기본값: 8000
# 다른 포트를 사용하려면 변경하세요 (예: 8080, 3000)
# 
# 주의: 이미 사용 중인 포트를 선택하면 서버 시작이 실패합니다.
API_PORT=8000

# 자동 리로드 설정
# true: 코드 변경 시 자동으로 서버 재시작 (개발 환경 권장)
# false: 수동 재시작 필요 (프로덕션 환경 권장)
# 
# 개발 중에는 true로 설정하여 편의성을 높이고,
# 프로덕션에서는 false로 설정하여 안정성을 확보하세요.
API_RELOAD=true

# CORS 허용 오리진
# 개발 환경: "*" (모든 도메인 허용)
# 프로덕션 환경: 특정 도메인만 허용 (쉼표로 구분)
# 예: "https://example.com,https://app.example.com"
# 
# 보안 주의: 프로덕션에서는 반드시 특정 도메인만 허용하세요.
CORS_ORIGINS=*

# =============================================================================
# 로깅 설정
# =============================================================================
# 애플리케이션의 로그 레벨 및 파일 경로를 설정합니다.

# 로그 레벨
# 사용 가능한 레벨 (낮은 순서대로):
# - DEBUG: 모든 디버그 정보 포함 (개발 환경)
# - INFO: 일반 정보 메시지 (기본값, 권장)
# - WARNING: 경고 메시지만 기록
# - ERROR: 오류 메시지만 기록
# - CRITICAL: 심각한 오류만 기록
# 
# 프로덕션에서는 INFO 또는 WARNING을 권장합니다.
LOG_LEVEL=INFO

# 로그 파일 경로
# 애플리케이션 로그가 저장될 파일 경로입니다.
# 상대 경로 또는 절대 경로를 지정할 수 있습니다.
# 
# 주의: 로그 디렉토리가 존재하지 않으면 자동으로 생성됩니다.
# 로그 파일은 자동으로 회전(rotation)되며, 크기가 커지면 새 파일로 분할됩니다.
LOG_FILE=./logs/app.log

# =============================================================================
# 데이터 디렉토리 설정
# =============================================================================
# 법률 문서 데이터가 저장되는 디렉토리 경로를 설정합니다.

# 원본 데이터 디렉토리
# 수집된 원본 법률 문서(JSON, PDF 등)가 저장되는 경로입니다.
# 이 디렉토리에는 다음 하위 디렉토리가 포함됩니다:
# - samples/: 샘플 데이터
# - dummy/: 테스트용 더미 데이터
# - raw/: 수집된 원본 데이터
DATA_DIR=./data

# 처리된 데이터 디렉토리
# 검증, 정제, 변환된 최종 JSON 데이터가 저장되는 경로입니다.
# 이 디렉토리의 데이터는 벡터 DB에 인덱싱됩니다.
# 
# 주의: 이 디렉토리는 데이터 처리 파이프라인에 의해 자동으로 생성됩니다.
PROCESSED_DATA_DIR=./data/processed

# =============================================================================
# 추가 설정 (필요시 주석 해제)
# =============================================================================
# 
# API 인증 설정
# API_KEY=your_api_key_here
# 
# Rate Limiting 설정
# RATE_LIMIT_PER_MINUTE=60
# 
# 세션 관리 설정
# SESSION_TIMEOUT=3600  # 초 단위 (기본값: 1시간)
# 
# 벡터 검색 설정
# MAX_SEARCH_RESULTS=10  # 최대 검색 결과 수
# SEARCH_SIMILARITY_THRESHOLD=0.7  # 유사도 임계값 (0.0 ~ 1.0)
# 
# 청킹 설정
# CHUNK_SIZE=1000  # 텍스트 청크 크기 (토큰 수)
# CHUNK_OVERLAP=200  # 청크 간 겹치는 토큰 수

