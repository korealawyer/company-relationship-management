# 🔍 개인소송 페이지 — 추가 기능 전체 목록
> 분석 기준: researcher.md | 전략 문서 5개 + 자동화 카탈로그 + AI 로드맵 + 로탑 IA 벤치마크 통합  
> 현재 `/personal-litigation/page.tsx` 완성도: **75%** (UI ✅, DB/자동화 ❌)

---

## 🔴 A그룹 — 지금 당장 추가해야 할 것 (P0)

### A-1. 기일 세부 등록 모달 (현재 날짜 필드만 있음)
| | |
|---|---|
| **현재** | `nextHearingDate` 날짜 1개 필드만 입력 |
| **추가할 것** | 기일 종류(변론/준비/조정/화해/선고) + 법정 호수 + **불변기일 토글** + 의뢰인 알림 ON/OFF |
| **핵심 이유** | 불변기일(항소기한 등) 누락 시 소멸시효 → 변호사 징계 |
| **연관 파일** | `CLIENT_LITIGATION_MASTER.md` §3-2 기일 등록 화면 |

### A-2. 기일 히스토리 타임라인 (CaseCard 내부)
| | |
|---|---|
| **현재** | 가장 가까운 기일 1개만 표시 |
| **추가할 것** | 모든 기일 타임라인 (과거 → 현재 → 예정) + 기일별 결과 메모 |

### A-3. 이해충돌 자동 체크 (`/conflict_check` 워크플로우 연동)
| | |
|---|---|
| **현재** | 없음 |
| **추가할 것** | 신규 사건 등록 시 상대방명(`opponent`) 자동 교차검사 → CLEAR/POTENTIAL/CONFLICT 뱃지 |
| **법적 의무** | **변호사법 제31조 — 이해충돌 금지** |
| **연관 파일** | `03_AUTOMATION_CATALOG.md` #16, `DOCUMENT_COMMENT_SYSTEM.md` §이해충돌 |

### A-4. 수임 계약서 자동 생성 트리거
| | |
|---|---|
| **현재** | 상태를 `consulting → preparing` 으로 바꿀 때 아무 일도 안 일어남 |
| **추가할 것** | 상태가 `retained(수임확정)`으로 변경 시 → 수임 계약서 PDF 초안 자동 생성 + 전자서명(이폼싸인) 요청 |
| **연관 파일** | `03_AUTOMATION_CATALOG.md` #7, `13_PAYMENT_CONTRACT_FLOW.md` |

### A-5. 미납 수임료 배너 + 자동 추적
| | |
|---|---|
| **현재** | 비용 UI만 표시 (미납 여부 없음) |
| **추가할 것** | 납부 상태 표시(미납/완납), 미납 배너, D+1/D+7/D+14 카카오 자동 알림 |
| **연관 파일** | `/billing_chase` 워크플로우, `03_AUTOMATION_CATALOG.md` #4~5 |

### A-6. Supabase DB 완전 연동
| | |
|---|---|
| **현재** | `localStorage` + 부분 `sbUpsert` |
| **추가할 것** | `personal_litigations` + `personal_clients` 테이블 + RLS 적용 |
| **연관 파일** | `CLIENT_LITIGATION_MASTER.md` §개인 의뢰인 DB 스키마 |

---

## 🟠 B그룹 — 1개월 내 추가해야 할 것 (P1)

### B-1. 기일 카카오 알림톡 자동 발송
| | |
|---|---|
| **추가할 것** | D-7 변호사 앱푸시, D-3 의뢰인 카카오 알림톡 (`HEARING_NOTICE_D3` 템플릿), D-1 SMS fallback |
| **불변기일** | D-14부터 대표변호사 CC 포함 전용 알림 (무음 금지) |
| **연관 파일** | `CLIENT_LITIGATION_MASTER.md` §3-2 자동 알림 스케줄 |

### B-2. 문서(서류) 업로드 + 분류 허브
| | |
|---|---|
| **현재** | 문서 목록 표시만 (업로드 UI 없음) |
| **추가할 것** | 드래그앤드롭 업로드 (Supabase Storage) + 생성주체 분류 **(우리제출 / 상대제출 / 법원문건 / 내부생성)** + OCR 자동 텍스트 추출 |
| **연관 파일** | `DOCUMENT_COMMENT_SYSTEM.md` §문서 생성주체 분류 |

### B-3. 인라인 문서 코멘트 스레드 (GitHub PR 스타일)
| | |
|---|---|
| **현재** | 없음 |
| **추가할 것** | 문서별 코멘트 스레드, 4가지 유형 (일반/승인/수정요청/공지), @태그, 해결처리(resolve) |
| **Sticky 효과** | 매일 사용 → 이탈 불가 → **1000억 ARR 핵심 기능** |
| **연관 파일** | `DOCUMENT_COMMENT_SYSTEM.md` §코멘트 시스템 |

### B-4. 의뢰인 포털 Magic Link 로그인 (`/my-cases` 연동)
| | |
|---|---|
| **현재** | `/my-cases` UI만 있음, 인증 없음 |
| **추가할 것** | 이메일 → Magic Link → 포털 접근 (Supabase Auth), 의뢰인이 사건 현황·기일·청구서 실시간 확인 |
| **연관 파일** | `CLIENT_LITIGATION_MASTER.md` §개인 의뢰인 포털 |

### B-5. 사건 종결 → 종결 리포트 자동 생성
| | |
|---|---|
| **현재** | 결과(승소/패소 등) 텍스트 입력만 |
| **추가할 것** | 상태가 `closed`로 전환 시 → 종결 리포트 PDF 일괄 자동 생성 |
| **연관 파일** | `DOCUMENT_COMMENT_SYSTEM.md` §이벤트→문서 자동 생성 매핑 |

### B-6. 만족도 설문 자동 발송
| | |
|---|---|
| **현재** | 없음 |
| **추가할 것** | 사건 종결 3일 후 → NPS 설문 이메일 자동 발송 |
| **연관 파일** | `03_AUTOMATION_CATALOG.md` #15 |

### B-7. 분할 납부 일정 관리
| | |
|---|---|
| **현재** | 수임료 총액 1개 필드만 |
| **추가할 것** | 분할 납부 비율 설정 (착수금/중도금/성공보수), 각 납기일 전날 자동 알림 |
| **연관 파일** | `03_AUTOMATION_CATALOG.md` #21 |

### B-8. 성공보수 청구 자동 트리거
| | |
|---|---|
| **현재** | 없음 |
| **추가할 것** | `result = '승소'` + `success_fee > 0` → 성공보수 청구서 자동 생성 |
| **연관 파일** | `03_AUTOMATION_CATALOG.md` #13 |

### B-9. 변호사 업무량 자동 배정 (라운드로빈)
| | |
|---|---|
| **현재** | 드롭다운으로 수동 선택 |
| **추가할 것** | 사건 등록 시 가장 적은 사건 담당 변호사에게 자동 배정 + 업무 과부하(15건+) 경고 |
| **연관 파일** | `03_AUTOMATION_CATALOG.md` #12, #17 |

### B-10. QR코드 결제 실제 연동
| | |
|---|---|
| **현재** | `/my-cases` 화면에 QR UI만 있음 |
| **추가할 것** | 토스페이먼츠 or 카카오페이 실제 결제링크 생성 → QR 코드 → 결제 완료 웹훅 → 납부 자동 처리 |
| **연관 파일** | `13_PAYMENT_CONTRACT_FLOW.md` |

---

## 🟡 C그룹 — 3개월 내 추가 (P2, 경쟁력 강화)

### C-1. 대법원 기일 자동 연동
| | |
|---|---|
| **현재** | 사건번호 수동 입력, 기일 수동 입력 |
| **추가할 것** | 사건번호 입력 → 대법원 나의사건검색 API → 기일·명령·송달 자동 동기화 |
| **로탑비교** | 로탑 IA 우세 영역 → **추격 필수** |
| **연관 파일** | `CLIENT_LITIGATION_MASTER.md` §로탑 우세 영역 |

### C-2. 손해배상 자동 계산기
| | |
|---|---|
| **현재** | 청구금액 수동 입력 |
| **추가할 것** | 자동차/산재/의료과실/기타 유형별 계산 → 일실이익, 위자료, 치료비 항목별 분리 입력 |
| **로탑비교** | 로탑 IA 가장 강한 기능 → **추격 필수** |

### C-3. 일간 모니터링 대시보드 배너
| | |
|---|---|
| **추가할 것** | 매일 오전 7시 자동 브리핑: 신규 기일, 기일 변경, 불변기일 D-30, 미납, 상대방 서면 접수 등 |
| **연관 파일** | `DOCUMENT_COMMENT_SYSTEM.md` Section 8 |

### C-4. TC/TM 타임시트 (업무시간 기록)
| | |
|---|---|
| **추가할 것** | 변호사가 사건별 작업 시간 입력 → 3분 내 청구서 자동 발송, 배당 자동 계산 |
| **연관 파일** | `DOCUMENT_COMMENT_SYSTEM.md` §TC/TM 타임시트 |

### C-5. 은행 입금 자동 수납 매칭
| | |
|---|---|
| **추가할 것** | 오픈뱅킹 API 연동 → 1시간 주기로 입금 자동 감지 → 해당 청구서 납부 자동 처리 |
| **연관 파일** | `DOCUMENT_COMMENT_SYSTEM.md` §bank_connections |

### C-6. 법률 서식 1,000종 자동 작성
| | |
|---|---|
| **현재** | 없음 |
| **추가할 것** | 소송 유형별 서식 선택 → 사건 데이터 자동 채움 → HWP/DOCX 다운로드 |
| **로탑비교** | 로탑 IA 3,000종 → 최소 1,000종 확보 필수 |

### C-7. 전자결재 라인 (결재 워크플로우)
| | |
|---|---|
| **추가할 것** | 수임 결정 → 담당변호사 1차 → 파트너변호사 최종 → 관리자 결재 라인 |
| **연관 파일** | `DOCUMENT_COMMENT_SYSTEM.md` §document_approvals |

### C-8. 홈택스 세금계산서 자동 발행
| | |
|---|---|
| **추가할 것** | TC 입력 완료 → 홈택스 API → 세금계산서 자동 발행 |
| **연관 파일** | `DOCUMENT_COMMENT_SYSTEM.md` §timesheets.tax_invoice_id |

### C-9. 사건 상태 변경 → 의뢰인 자동 알림
| | |
|---|---|
| **현재** | 상태 변경 UI만 있음, 알림 없음 |
| **추가할 것** | 상태 변경 → 의뢰인 카카오 알림 + 포털 배지 자동 업데이트 |
| **연관 파일** | `03_AUTOMATION_CATALOG.md` #11 |

### C-10. 문서 버전 레드라인 비교
| | |
|---|---|
| **추가할 것** | 서면 v1↔v2 비교 뷰어, 삭제문 취소선(빨강), 추가문(초록), 변경 조항 자동 코멘트 |
| **연관 파일** | `DOCUMENT_COMMENT_SYSTEM.md` §UX 인터랙션 #4 |

---

## 🤖 D그룹 — AI 기능 (P3, 경쟁 해자 구축)

### D-1. AI 판례 검색 (RAG)
| | |
|---|---|
| **추가할 것** | 사건 키워드 → 대법원 오픈API + pgvector → 유사 판례 5개 요약 카드 |
| **기술스택** | GPT-4o + 대법원 오픈API + pgvector |
| **연관 파일** | `07_AI_PRODUCT_ROADMAP.md` Phase 1-1 |

### D-2. 준비서면 AI 초안 생성
| | |
|---|---|
| **추가할 것** | 사건 기본정보 + 의뢰인 주장 입력 → 판례 자동 검색 → 서면 구조(사실관계→쟁점→법령→결론) 초안 |
| **연관 파일** | `07_AI_PRODUCT_ROADMAP.md` Phase 2-2 |

### D-3. 승소 확률 예측 모델
| | |
|---|---|
| **추가할 것** | 사건 유형 + 증거강도(1~5) + 청구금액 → ML 예측 → 승소확률(%)·리스크요인 3개·예상 기간 |
| **연관 파일** | `07_AI_PRODUCT_ROADMAP.md` Phase 3-1 |

### D-4. AI 음성 상담록 (STT)
| | |
|---|---|
| **추가할 것** | 상담 녹음 업로드 → STT + 화자분리 → 상담록 자동 텍스트화 → 사건 메모로 자동 저장 |
| **로탑비교** | 로탑 강점 기능 → **로탑과 동등 + 자동저장으로 차별화** |

### D-5. 계약서/서면 위험 조항 탐지
| | |
|---|---|
| **추가할 것** | 문서 업로드 → GPT-4o → 위험 조항 하이라이트 + 위험도(High/Medium/Low) + 수정 제안 |
| **연관 파일** | `07_AI_PRODUCT_ROADMAP.md` Phase 1-2 |

### D-6. 의뢰인 포털 AI 사건 요약
| | |
|---|---|
| **추가할 것** | `/my-cases` 포털에서 의뢰인이 "내 사건 지금 어떻게 돼요?" 질문 → AI가 현황 자동 요약 메시지 생성 |

### D-7. 법령 변경 모니터링
| | |
|---|---|
| **추가할 것** | 법제처 API → 사건 관련 법령 변경 감지 → 담당 변호사 자동 알림 |
| **연관 파일** | `07_AI_PRODUCT_ROADMAP.md` Phase 4-2 |

---

## 📊 전체 우선순위 요약

| 그룹 | 기능 수 | 스프린트 | 핵심 임팩트 |
|------|--------|---------|------------|
| **A (P0 — 즉시)** | 6개 | 이번 주~2주 | DB연동, 이해충돌, 불변기일 보호 |
| **B (P1 — 1개월)** | 10개 | 2~4주 | 알림자동화, 포털연동, 결제 |
| **C (P2 — 3개월)** | 10개 | 5~12주 | 대법원연동, 손배계산기, 서식 |
| **D (P3 — AI)** | 7개 | 3~6개월 | 판례검색, 승소예측, 서면AI |
| **합계** | **33개** | — | — |

---

## 🏆 로탑 IA 대비 현재 경쟁력

| 기능 | 로탑 IA | 지금 우리 | 목표 |
|------|---------|---------|------|
| 기일 자동 알림 | ✅ SMS | A-1 + B-1 구현 후 ✅ 카카오 | 🟢 우위 |
| 이해충돌 체크 | ✅ 모바일 즉석 | A-3 구현 후 ✅ | 🟢 동등 |
| 손해배상 계산기 | ✅ 자·산·의·기 | C-2 구현 후 ✅ | 🟡 추격 |
| 대법원 기일 연동 | ✅ | C-1 구현 후 ✅ | 🟡 추격 |
| 법률 서식 | ✅ 3,000종 | C-6 구현 후 ✅ 1,000종 | 🟡 추격 |
| 개인 의뢰인 포털 | ❌ | B-4 구현 후 ✅ | 🟢 **차별화** |
| AI 판례 검색 | ❌ | D-1 구현 후 ✅ | 🟢 **차별화** |
| 문서 인라인 코멘트 | ❌ | B-3 구현 후 ✅ | 🟢 **차별화** |

---

## 🎯 즉시 시작 권장 순서

```
이번 주:
  [1] personal_litigations DB 테이블 생성 + RLS (A-6)
  [2] 기일 세부 등록 모달 (A-1) — 불변기일 토글 포함
  [3] 이해충돌 자동 체크 (A-3) — 법적 의무 최우선

2주 내:
  [4] 기일 카카오 알림톡 D-3 자동 발송 (B-1)
  [5] 의뢰인 포털 Magic Link 로그인 (B-4)
  [6] 문서 업로드 허브 (B-2)
  [7] 미납 자동 추적 (A-5)

1개월:
  [8] 인라인 문서 코멘트 스레드 (B-3)
  [9] QR 결제 실제 연동 (B-10)
  [10] 대법원 기일 자동 연동 (C-1)
```

---
*기반 문서: `CLIENT_LITIGATION_MASTER.md` · `DOCUMENT_COMMENT_SYSTEM.md` · `03_AUTOMATION_CATALOG.md` · `07_AI_PRODUCT_ROADMAP.md` · `personal-litigation/page.tsx` · `mockStore.ts`*
